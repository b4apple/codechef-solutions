#include<bits/stdc++.h>

#define int long long int

using namespace std;

main()
{
    ios_base::sync_with_stdio(NULL);
    cin.tie(NULL);

    int t, a, b;
    cin >> t;
    while(t--)
	{
		cin >> a >> b;
		cout << a + b << "\n";
	}
}